backend-docker-release:
    image: docker:latest
    before_script: 
        - "cd backend"
        - "echo $GCLOUD_SERVICE_ACCOUNT | docker login -u _json_key --password-stdin https://eu.gcr.io"
    script: 
        - "make release"
    only:
        changes:
          - backend/**/*
